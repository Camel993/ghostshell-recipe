# GhostShell — copy-ready panel

**Élő panel:** https://camel993.github.io/ghostshell-recipe/

Egyfájlos, böngészőben futó panel a GhostShell GFN-swap metódushoz:
minden mező kattintásra a vágólapra kerül (PATCH URL + cél útvonal egy gombbal).

A fájl generált: a privát `Camel993/ghostshell` repó `scripts/sync-recipe.sh`
lépése tölti fel minden build után (verzió + content-length automatikusan szinkronban).

Kód és binárisok: privát repók. Ez a repó csak a panelt tartalmazza.
